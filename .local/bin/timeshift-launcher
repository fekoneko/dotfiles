#!/bin/sh

# this ignores env variables that contain spaces
# new lines in env variables can break things too
# shellcheck disable=SC2046
pkexec env $(env | grep -v ' ') /usr/bin/timeshift-launcher "$@"
